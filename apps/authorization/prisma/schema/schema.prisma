generator client {
  provider = "prisma-client-js"
  output   = "../../../../node_modules/.prisma/client/authorization/"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_URL")
}
